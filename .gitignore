# Claude Code local session state (contains local settings / permissions)
.claude/

# Manuscript source withheld from the public repo until approved for release.
# Kept on the author's disk; regenerate figures/results from the scripts below.
mwm_audit_paper.tex
mwm_audit_paper.pdf
mwm_revised_full_corrected.tex

# Third-party reference paper (LeWorldModel) — not ours to redistribute.
LeWorldModel.pdf

# Superseded short-history checkpoints + live-trading heads (no paper number
# uses these; kept on disk for the live-trading path, out of the repo).
experiments/checkpoints/
# last_model.pt is the final-epoch weights; the paper uses best_model.pt only.
experiments/checkpoints_long/*/last_model.pt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# LaTeX build intermediates (the .tex and the built .pdf are tracked)
*.aux
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg
*.toc
mwm_audit_paper.log
mwm_revised_full_corrected.log

# OS
.DS_Store
Thumbs.db
desktop.ini
